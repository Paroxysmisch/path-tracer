#pragma once

namespace pathtracer {

    // epsilon for equivalence of floating point numbers
    inline constexpr float epsilon = 0.00001;

    inline constexpr size_t height = 1080;

    inline constexpr size_t width = 1920;
    
}
